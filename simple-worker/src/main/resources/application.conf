include "common-config.conf"

akka {
  remote {
    netty.tcp {
      hostname = "127.0.0.1"
      hostname = ${?WORKER_HOST}
      port = ${WORKER_PORT}
    }
  }

  cluster { #//<co id="cluster_config"/>
    roles = ["worker"]
  }
}
